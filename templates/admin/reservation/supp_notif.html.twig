<form method="post" action="{{ path('app_reservations_delete', {'id': r.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r.id) }}">
    <button class="btn-danger btn-circle"><i class="fas fa-trash"></i></button>
</form>
